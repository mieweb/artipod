# .attic

Dead code preserved with context (see repo copilot-instructions: Dead Code Management).

| File | Retired | Why |
|---|---|---|
| shell.ts | Phase 1 of just-bash-plan.md | Hand-rolled command parser (`ls`/`cd`/`cat`/`git`/`edit`), replaced by the just-bash interpreter behind `lib/sandbox/createSandbox()`. Kept for reference of the original PoC command surface. |
