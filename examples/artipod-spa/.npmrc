; file: deps are COPIED into node_modules (not symlinked). Load-bearing:
; a symlinked @artipod/core would resolve @zenfs/core outside this app and
; Next would silently bundle that copy. scripts/export-static.mjs refreshes
; the copy every export.
install-links=true
