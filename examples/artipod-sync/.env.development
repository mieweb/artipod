# Dev-only registry allowlist for the /api/oci relay (Decision #4: the
# default is EMPTY = deny all; this file is what the hosted demo enables).
# docker.io needs both its API host and its token service.
ARTIPOD_OCI_ALLOWED_HOSTS=registry-1.docker.io,auth.docker.io,ghcr.io,quay.io
