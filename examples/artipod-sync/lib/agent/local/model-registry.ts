/** Re-export shim — moved to @artipod/core/agent (plan Phase 2; one release). */
export {
  CURATED_MODELS,
  DEFAULT_LOCAL_MODEL,
  listLocalModels,
  modelInfo,
} from '@artipod/core/agent';
export type { LocalModelInfo, OnnxDtype } from '@artipod/core/agent';
