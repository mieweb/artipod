/**
 * Re-export shim — the sandbox core moved to @artipod/core/sandbox
 * (artipod-layer-plan Phase 2). This shim lives for one release; import from
 * the package directly in new code.
 */
export * from '@artipod/core/sandbox';
