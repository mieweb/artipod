/**
 * Re-export shim — moved to @artipod/core/sandbox (plan Phase 2; one release).
 */
export type {
  CompletionResult,
  Sandbox,
  SandboxExecOptions,
  SandboxExecResult,
  ZenFsLike,
} from '@artipod/core/sandbox';
